* Implement proper exception objects instead of just throwing strings.
* Properly access `chrome.runtime.lastError.message` property when throwing.
* Implement invasions.
* Implement sounds.
* Implement desktop notifications.
* Add Images for blueprints (mod cards are a possibility as well).
* Use console.warn() when logging exceptions.